data <- dget("PDL1_tumor.Rd")
str(data)
colnames(data)
gbar <- function(var, data = data){
g <- ggplot(data = data, aes(x = var, fill = var)) +
geom_bar(color = "black") +
ylab("No. patients") +
gtheme + theme(legend.position = "none")
print(g)
descriptive.categorical(var)
}
data <- dget("PDL1_tumor.Rd")
View(data)
library(psych)
?psych
?wilcox.test
data <- dget("PDL1_tumor.Rd")
str(data)
colnames(data)
col_factor <- c(1:23)
col_factor
col_factor <- colfactor[-1]
col_factor <- col_factor[-1]
col_factor
colnames(data)
logistic.table()
predictors <- list(
"Patient's sex" = gender,
"Patient's age group" = age_group,
"High pT (>pT2) stage" = high_pt
)
